const useGetHost: () => string = () => {
    // let host = window.location.host
    // host = host.slice(0, host.length - 4)
    // host = host + 4000

    return "https://workout-central-server-k2sr4c3o2-jehus-projects-79647ffa.vercel.app"
}

export default useGetHost